# CLAUDE.md

개인 포트폴리오 웹사이트 (https://jhyungit.github.io/).
React 19 + Vite 7, GitHub Pages 배포. 백엔드 개발자 취업용.

**주 독자는 면접관이다.** 면접 직전에 "이 사람 뭐 했더라"를 확인하러 들어온다.
요약은 PDF 이력서가 이미 한다. 이 사이트의 역할은 **PDF가 지면 제약으로
담을 수 없는 증거**다 — 코드, 실행계획 출력, 아키텍처 다이어그램, 의사결정 근거.

---

## 이 리포에서 가장 중요한 규칙

**사이트에 표시되는 모든 문장은 검증된 사실이어야 한다.**
틀린 문장 하나가 답할 수 없는 면접 질문을 만든다.

### 콘텐츠를 쓰거나 수정하기 전에 — 예외 없음

1. **`PROFILE.md`를 먼저 읽는다.** 확정 수치와 귀속 경계가 여기 있다
2. 쓰려는 문장의 근거를 PROFILE.md에서 찾는다
3. **못 찾으면 쓰지 않는다.** 사용자에게 묻는다

### 동기화 방향은 단방향이다

사실이 바뀌면 `PROFILE.md`를 먼저 고친다.
그다음 `docs/resume.html` → `docs/portfolio.html` → `src/data/` →
GitHub 프로필 README 순으로 맞춘다.

**역방향으로 고치지 않는다.** 산출물에서 발견한 새 사실은 먼저 PROFILE.md에
반영한 뒤 나머지에 전파한다.

```
PROFILE.md  (원본)
    ↓
docs/resume.html  →  docs/portfolio.html  →  src/data/  →  GitHub README
```

**단, 두 PDF의 역할이 다르다.** `docs/resume.html`은 3페이지 백엔드 요약이라
프로젝트를 선별해 싣고(YORR · TripCrew · yorr-arcade · IBK · 달리셔스),
`docs/portfolio.html`이 전체 근거를 싣는다.
**무신사·냐암냐암이 이력서에 없는 것은 누락이 아니라 의도된 것이다.
이력서에 추가하지 않는다.**

같은 사실이 4개 산출물에 흩어져 있어 과거에 수동으로 불일치를 잡아야 했다
("28종" vs "30여 종", "채택" vs "채택·이식", yorr.site vs vercel 주소 등).
PROFILE.md는 그 사본이 5개로 늘어나는 것을 막기 위한 원본이다.
**산출물끼리 대조해 판단하지 말고 PROFILE.md를 기준으로 양쪽을 맞춘다.**

### 절대 하지 말 것

- **수치를 추정하거나 반올림하지 않는다.** "약 26배"는 확정값이다.
  "20배 이상", "30배 가까이"로 바꾸지 않는다
- **기술 스택을 유추하지 않는다.** "Redis 컨테이너가 있으니 Redis로
  실시간 처리했을 것"이라는 추론이 실제로 사이트에 사실 오류를 만들었다
  (`src/data/project.js` TripCrew 실시간 설명)
- **팀 프로젝트의 팀원 작업을 사용자 성과로 쓰지 않는다.**
  PROFILE.md §4의 "담당 아님" 표를 매번 확인한다
- **형용사로 자리를 채우지 않는다.** "성실한", "열정적인", "Fast learner"는
  반증 불가능하므로 정보가 0이다. 수치나 구체적 행위로 대체한다
- **자기평가 척도(별점 등)를 만들지 않는다.** 검증 불가능하다
- **PDF 원본(`docs/resume.html`, `docs/portfolio.html`)을 임의로 수정하지
  않는다.** 사실 원본 계층에 속한다. 변경은 사용자 지시로만
- **PROFILE.md §6 "미확인" 목록의 내용은 쓰지 않는다.** 확인 전까지 금지

### 자주 틀리는 지점 — 매번 확인

| 항목 | 사실 |
|---|---|
| YORR WebSocket | raw handler (`GameWebSocketHandler`) |
| TripCrew WebSocket | **STOMP** |
| YORR 라운드 상태 | **InMemory**. Redis 아님 |
| TripCrew 실시간 협업 | **인메모리 SimpleBroker**. Redis Pub/Sub 아님 |
| 소셜 로그인 | YORR = Google / Kakao (**팀원 구현, 본인 성과 아님**) / TripCrew = Kakao / Naver |
| 네트워크 요청 패턴 분석 | **무신사 프로젝트만** |

---

## 디자인 규칙

### 색 — 2~3개로 제한
- `--color-primary` (#4f8cff), `--color-accent` (#7c3aed), 중립 회색 스케일
- **새 색조를 추가하지 않는다.** 강조는 굵기와 여백으로 한다
- 예외: 수상 배지 금색(`--award-badge-bg`/`--award-badge-border`).
  기존에 리터럴로 있던 것을 토큰화한 것이며 사용처는 배지 1곳뿐이다.
  다른 곳에 쓰지 않는다
- ⚠️ **라이트 `--color-primary`(#4f8cff)를 더 밝게 바꾸지 않는다.**
  스킬 활성 탭이 이 위에 `--text-strong`을 얹어 **4.55:1**로 AA 기준(4.5:1)에
  겨우 걸쳐 있다. 조금만 밝아져도 미달로 떨어진다.
  바꿔야 한다면 대비를 다시 계산하고 짝 글자색도 함께 조정한다
  (흰 글자는 이 파랑 위에서 3.22:1로 이미 미달이라 쓸 수 없다)
- 색을 추가할 때는 **라이트(`:root`)와 다크(`[data-theme="dark"]`) 양쪽에**
  추가한다. 한쪽만 하면 다크모드가 깨진다
- 라이트와 다크에서 **같은 색조를 유지한다.** 다크에서 그라데이션이 분홍으로
  바뀌는 문제가 있다 (`src/App.css`의 `--gradient-accent`)

### 금지 — 클리셰
- **타이핑 애니메이션**
- **터미널 흉내**: `$` / `➜` 프롬프트 기호, 네온 초록, macOS 창 버튼(원 3개),
  커서 깜빡임, 타이핑되며 나타나는 코드
- 매트릭스 폴링, ASCII 아트
- **과한 스크롤 애니메이션.** 스크롤마다 요소가 페이드인하지 않는다
- 숫자 카운트업 애니메이션
- 그라데이션 밑줄 장식

### 모노스페이스는 제한적으로
- 코드, 실행계획 출력, 수치(`0.473s → 0.018s`), 파일명·식별자에만
- 본문·제목·라벨에는 쓰지 않는다

### 증거 인용 vs 연출 — 구분할 것

| 해야 함 (실제 산출물 인용) | 하지 말 것 (터미널 흉내) |
|---|---|
| 모노스페이스 폰트 | `$` / `➜` 프롬프트 기호 |
| 실제 EXPLAIN 출력 텍스트 | 네온 초록 글씨 |
| 파일명·쿼리명 캡션 | macOS 창 버튼 |
| 차분한 배경 (`--bg-sunken` 계열) | 커서 깜빡임 |
| 한 줄씩 정적으로 표시 | 타이핑되며 나타나는 연출 |

**원칙: 인용은 하고, 연출은 하지 않는다.**

### 스타일 값
- **`src/App.css`의 CSS 변수만** 사용한다. 색·간격·radius·폰트 크기를
  하드코딩하지 않는다
- **색상값은 hex·rgb()·rgba()·hsl()·hsla()·색 키워드를 모두 센다.**
  과거 이 항목이 hex만 세는 바람에 `rgba()`가 규칙을 통째로 빠져나갔다.
  (`transparent`·`currentColor`는 색상값으로 세지 않는다)
- 실측 (2026-08-31 전수 조사):
  - 섹션/컴포넌트 CSS **31건** (hex 6 + rgba 25)
  - `src/components/Diagram/` JSX의 SVG presentation attribute **85건**
  늘리지 말 것. 새 색을 쓰기 전에 기존 토큰에 같은 값이 있는지 먼저 찾는다
- `rgba(79, 140, 255, *)`의 알파값이 11단계로 흩어져 있다.
  단계를 줄이는 것은 시각 변경을 동반하므로 별도 과제로 다룬다
- 섹션 배경은 클래스 선택자로 지정한다.
  `section#skill` 같은 ID 선택자를 새로 만들지 않는다
- 애니메이션을 추가하면 `App.css`의 `prefers-reduced-motion` 블록에도
  반드시 등록한다

---

## 코드 규칙

- 컴포넌트 폴더 규칙: `Xxx/Xxx.jsx` + `Xxx/Xxx.css`
- **콘텐츠는 JSX에 직접 쓰지 않고 `src/data/`에 둔다.** 뷰는 데이터를 렌더만 한다
- 프로필 상수(이메일·GitHub URL·이름·직군)는 `src/data/profile.js` 한 곳에서만
  정의한다. 현재 이메일이 3곳, GitHub URL이 5곳에 하드코딩돼 있다 —
  발견하면 정리
- **데이터 배열의 인덱스에 로직을 결합하지 않는다.**
  `CareerSection.jsx`가 `idx === 0 ? ssafyRef : ...`로 연도 배지를 매핑하고
  있다. 이런 패턴을 새로 만들지 않는다
- 반복되는 UI(태그·배지·카드·섹션 헤더·모달)는 `components/`에 프리미티브로
  만든다. 섹션마다 다시 만들지 않는다
  (현재 태그 pill이 6개 클래스로 중복 정의돼 있다)

### 접근성 — 회귀 금지
- 모달: 포커스 트랩 + Esc 닫기 + 열기 전 포커스 복원
- 인터랙티브 요소에 `aria-label`
- `:focus-visible` 스타일을 제거하지 않는다
- 색 대비 WCAG AA (4.5:1) 이상

### 이미지
- **커밋 전 용량 확인.** 현재 빌드 산출물 11MB 중 이미지가 10MB다
- 로고는 SVG 우선. PNG는 최대 20KB
- 스크린샷은 WebP, 최대 200KB
- 콘텐츠 이미지에 `loading="lazy"`
- 미해결: `oracledb.png`가 1.57MB로 약 48px에 표시된다

---

## 하지 말 것 — 이 리포 한정

- `npm run dev`를 임의로 띄우지 않는다. 사용자가 요청할 때만
- **지시하지 않은 커밋/푸시를 하지 않는다.**
  푸시하면 GitHub Actions가 즉시 프로덕션에 배포한다
- `dist/`를 커밋하지 않는다 (gitignore 대상)
- `.github/workflows/deploy.yml`을 임의로 수정하지 않는다. 동작 중이다
- 새 의존성을 임의로 추가하지 않는다.
  현재 런타임 의존성은 react, react-dom뿐이다 (유지 목표)

---

## 작업 흐름

```bash
npm run dev      # 사용자 요청 시에만
npm run build    # 빌드 검증
npm run lint     # ESLint
```

`main`에 푸시 → GitHub Actions → GitHub Pages. **푸시 = 배포다.**

---

## 참고 문서

- `PROFILE.md` — **사실 원본. 콘텐츠 작업 전 필독**
- `docs/resume.html` — 이력서 PDF 소스 (git 미추적)
- `docs/portfolio.html` — 포트폴리오 PDF 소스 (git 미추적)
- `docs/redesign-proposal.md` — 재설계 진단·제안 (2026-08, git 미추적)
